<section class="first-section" id="single-post">
    <div class="container">
        {{ admin_message|raw }}
        <p><a href="index.php?action=listPosts">Retour à la liste des billets</a></p>

        <div class="news">
            <h3>
                {{ post.title }}
                <em class="post-date">le {{ post.last_modif_date_fr }} par {{ post.author_first_name }} {{ post.author_last_name }}</em>
            </h3>
            {{ admin_links|raw }}
            <p>
                <br><span class="post-intro">{{ post.intro|raw }}</span><br><br>
                {{ post.content|raw }}
            </p>
        </div>
    </div>
</section>
